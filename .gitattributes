# Unified diff context lines intentionally begin with a space before Go tabs.
# Validate the applied source with git apply --check, gofmt and Go tests instead.
tools-image/envd-overlay/upstream.patch -whitespace
